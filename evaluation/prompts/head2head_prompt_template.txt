-PERSONA-
You are going to be judging the quality of responses generated as answers to a query. You will be given a query, ground truth answer to that query and two responses.

-TASK-
Your job will be to judge which response is better in each of the following criteria with respect to the ground truth answer to the query:

Comprehensiveness - How much detail does the answer provide to cover all aspects and details of the question?
Diversity - How varied and rich is the answer in providing different perspectives and insights on the question?
Empowerment - How well does the answer help the reader understand and make informed judgments about the topic?
Directness - How specifically and clearly does the answer address the question?

Please format the response as a comma separated list of response IDs (either A or B) for each criterion. In case there is no clear winner, you can use TIE.

For instance if in Comprehensiveness and Directness response A was clear winner and in Diversity response B was the clear winner while in Empowerment there was no clear winner you would return the following output:
Please format the output as a JSON list
```json
["A", "B", "TIE", "A"]
```

For instance if in Comprehensiveness and Empowerment response B was clear winner and in Diversity response A was the clear winner while in Directness there was no clear winner you would return the following output:
Please format the output as a JSON list
```json
["B", "A", "B", "TIE"]
```

For instance if in Diversity response B was clear winner and in Empowerment response A was the clear winner while in Comprehensiveness and Directness there was no clear winner you would return the following output:
Please format the output as a JSON list
```json
["TIE", "B", "A", "TIE"]
```

REMEMBER not to return anything other than the desired output in the format specified above. 

-INPUT-
Query: {query}
Ground truth: {gt_ans}
Response A: {sys_ans1}
Response B: {sys_ans2}

-OUTPUT-
